services:
  backend:
    build:
      dockerfile: Dockerfile
    healthcheck:
      start_period: 20s 

  frontend:
    build:
      dockerfile: Dockerfile
      args:
        - API_URL=http://backend:8080/api/v1
